CREATE DATABASE webappdb;

USE webappdb;

CREATE TABLE `questions` (
	`question_id` INT NOT NULL,
	`question` TEXT NOT NULL,
	PRIMARY KEY (`question_id`));

CREATE TABLE `electees` (
	`electee_id` INT NOT NULL,
	`first_name` VARCHAR(30) NOT NULL,
	`last_name` VARCHAR(30) NOT NULL,
	PRIMARY KEY (`electee_id`));

CREATE TABLE `answers` (
	`electee_id` INT NOT NULL,
	`question_id` INT NOT NULL,
	`answer` INT,
	PRIMARY KEY (`electee_id`,`question_id`));

GRANT ALL ON webappdb.* TO user@localhost identified by 'password';

insert into electees(electee_id,first_name,last_name) VALUES ('1','First','Amonglast');
insert into questions (question_id,question) VALUES ('1','Testing...?');


